apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bunkbed.fullname" . }}-test-connection"
  labels:
    {{- include "bunkbed.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "bunkbed.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
